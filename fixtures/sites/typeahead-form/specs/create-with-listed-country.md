# Create with a listed country

Submit stores a painted country row, not a typed string with an empty hidden id.

```clickmonkey
open home
fill page.country Norway
click page.submit
expect path /ok.html
```
