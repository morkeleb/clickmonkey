---
title: "Q-06 Same tab title on every route"
permalink: /findings/Q-06/
---

# Q-06 — Same tab title on every route

**Chapter:** quality  
**Rule:** `document-title-same`  
**Spec:** [Same tab title on every route](https://morkeleb.github.io/clickmonkey/findings/Q-06/)

Every route uses the same tab title, so screen readers, search, and people with many tabs cannot tell pages apart.

Each route needs a title that names that page. Not this: a missing document.title (WCAG 2.4.2); different records on one route sharing a title (Q-07).

This id is **stable**. Reports link here so T/V/Q classes do not shuffle when a soak ranks differently.
